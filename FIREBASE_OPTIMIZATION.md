# Firebase書き込み量最適化の提案

## 現状の問題点

### 書き込み量の内訳（1ユーザー・1セッション）
- **playページ**: 40-50回（カード選択ごと）
- **play2ページ**: 50-100回（自動保存）
- **waitingページ**: 30-80回（カード移動ごと + 移動履歴）
- **合計**: 120-230回/ユーザー

### 4人プレイ時
- **480-920回の書き込み/セッション**
- 無料枠（20,000回/日）で約20-40セッション可能

---

## 🔧 優先度別の改善案

### 🔴 **優先度：高（即時対応推奨）**

#### 1. play2の自動保存を削減
**現状**: カード選択・理由入力の度に1秒後に保存
```typescript
// 問題のコード
useEffect(() => {
  if (isInitialized) {
    debouncedAutoSave(); // 頻繁に実行される
  }
}, [wantSelected, dontSelected, reasons, planName]);
```

**改善案A: 自動保存を削除（推奨）**
- リロード対応は`finalSelections`から復元
- 保存は「終了して次へ」ボタンクリック時のみ
- **削減効果: 50-100回 → 2-3回**

**改善案B: デバウンス時間を延長**
- 1秒 → 5-10秒に変更
- **削減効果: 50-100回 → 10-20回**

#### 2. waitingページのカード移動保存を最適化
**現状**: カードを移動する度に保存

**改善案A: バッチ保存（推奨）**
- カード移動は即座に反映（ローカル状態）
- 5-10秒ごと、または5回の移動ごとにまとめて保存
- **削減効果: 30-80回 → 5-10回**

**改善案B: 移動履歴を削除**
- `saveMovementHistory`を削除（実験用なら）
- **削減効果: 15-40回削減**

### 🟡 **優先度：中**

#### 3. playページの「元に戻す」を最適化
**現状**: ログ削除 + カード復活で2回の操作

**改善案: ソフトデリート**
- ログを削除せず、`deleted: true`フラグを追加
- **削減効果: 削除操作分を半減**

#### 4. リアルタイム更新の最適化
**現状**: onSnapshotで常時監視

**改善案: 必要な時だけ取得**
- 初回ロード時とページ遷移時のみ取得
- リアルタイム更新が不要な場合は`getDoc`を使用
- **削減効果: 読み取り回数を削減（書き込みには影響なし）**

### 🟢 **優先度：低（将来的な改善）**

#### 5. ローカルストレージの活用
- リロード対応をFirestoreではなくlocalStorageで実装
- Firestoreへの保存は最終確定時のみ

#### 6. オフラインキャッシュの活用
- Firebase SDKのオフライン永続化を有効化
- ネットワーク障害時の対応も改善

---

## 📋 実装の優先順位

### フェーズ1（即時対応）
1. ✅ **play2の自動保存を削除** → 最も効果的
2. ✅ **waitingのバッチ保存** → 2番目に効果的

### フェーズ2（次回のアップデート）
3. 移動履歴の削除または最適化
4. playページの最適化

### フェーズ3（将来的な改善）
5. ローカルストレージの活用
6. オフライン対応

---

## 🎯 改善後の期待効果

### 書き込み量（1ユーザー・1セッション）
- **現状**: 120-230回
- **改善後**: 50-70回
- **削減率**: 約60-70%削減

### 4人プレイ時
- **現状**: 480-920回
- **改善後**: 200-280回
- **効果**: 無料枠で50-100セッション可能（2-3倍）

---

## ⚡ 今すぐできる対応

### 最小限の変更で最大の効果

```typescript
// play2/page.tsx
// ❌ 削除: 自動保存のuseEffect
// useEffect(() => {
//   if (isInitialized) {
//     debouncedAutoSave();
//   }
// }, [wantSelected, dontSelected, reasons, planName, isInitialized, debouncedAutoSave]);

// ✅ 保持: handleFinish内の保存のみ残す
```

```typescript
// waiting/page.tsx
// ❌ 変更前: カード移動ごとに保存
const attemptDropToCategory = () => {
  // ...
  saveCategories(normalized); // 毎回保存
};

// ✅ 変更後: デバウンス付き保存
const [pendingSave, setPendingSave] = useState(false);
useEffect(() => {
  if (!pendingSave) return;
  const timer = setTimeout(() => {
    saveCategories(categories);
    setPendingSave(false);
  }, 3000); // 3秒後にまとめて保存
  return () => clearTimeout(timer);
}, [pendingSave, categories]);
```

---

## 📊 モニタリング方法

Firebase Consoleで以下を確認：
1. **使用量タブ** → ドキュメントの書き込み数
2. **Firestore → 使用量** → 日次の書き込み回数
3. アラート設定: 18,000回/日を超えたら通知

---

## ⚠️ 注意事項

- リロード対応が必要な場合は、最終保存データから復元する実装を維持
- ユーザー体験を損なわないよう、保存中のインジケーターを表示
- バッチ保存時は「保存していないデータがあります」等の警告を表示
