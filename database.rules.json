{
  "rules": {
    ".read": true,
    ".write": true,
    "presence": {
      "$roomId": {
        "$uid": {
          ".read": true,
          ".write": true,
          "lastActive": { ".validate": "newData.isNumber() && newData.val() > 0" }
        }
      }
    }
  }
}
